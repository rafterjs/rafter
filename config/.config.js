export default {
    autoloader: {
        directory: `${__dirname}/../lib`,
        config: `.config.js`,
        routes: `.routes.js`,
        middleware: `.middleware.js`,
        services: `.services.js`
    }
};
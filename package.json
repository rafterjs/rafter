{
    "name": "rafter",
    "version": "0.0.1",
    "contributors": [
        {
            "name": "Josh Stuart",
            "email": "joshstuartx@gmail.com"
        }
    ],
    "license": "MIT",
    "scripts": {
        "dev": "nodemon --watch ./lib --watch ./config --watch ./example --watch ./rafter.js ./example/rafter-example.js --exec babel-node",
        "test": "TODO",
        "build-server": "babel ./index.js --out-dir ./dist && babel ./rafter.js --out-dir ./dist && babel --include-dotfiles ./lib --out-dir ./dist/lib && babel --include-dotfiles  ./config --out-dir ./dist/config && babel --include-dotfiles  ./example --out-dir ./dist/example",
        "clean": "rm -rf ./dist && mkdir ./dist",
        "build": "npm run clean && npm run build-server",
        "prepublish": "npm run build"
    },
    "dependencies": {
        "box-di-autoloader": "^0.2.0",
        "express": "4.16.2"
    },
    "description": "A slightly opinionated JS framework that makes building apps easy and testable",
    "main": "dist/index.js",
    "devDependencies": {
        "@babel/cli": "^7.0.0-beta.39",
        "@babel/core": "^7.0.0-beta.39",
        "@babel/node": "^7.0.0-beta.39",
        "@babel/plugin-proposal-object-rest-spread": "^7.0.0-beta.39",
        "@babel/preset-env": "^7.0.0-beta.39",
        "babel-eslint": "^7.2.3",
        "eslint": "^4.17.0",
        "nodemon": "^1.14.12"
    },
    "engines": {
        "node": "9"
    }
}
